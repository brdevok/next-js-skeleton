/**
 * Enum representing color values that correspond directly to SASS variables.
 *
 * This enum is intended for scripting purposes and provides access to the same
 * color values defined in the SASS variables located in `styles/variables/colors.scss`.
 * Synchronizing these values ensures consistency between the styles and scripts.
 *
 * @enum {string}
 */
export const enum Color {
	Blue = '#0000ff',
}